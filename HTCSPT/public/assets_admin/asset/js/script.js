$(window).scroll(function () {
    // if ($(window).scrollTop() >= 81) {
    //     $('.header').addClass('fixed-menu');
    // } else {
    //     $('.header').removeClass('fixed-menu');
    // }
});